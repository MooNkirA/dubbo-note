spring:
  application:
    name: dubbo-provider
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
dubbo:
  provider: # 配置全局负载均衡策略
    loadbalance: random